define service{
        use                             standard-service         
        host_name                       {{ ansible_hostname }}
        service_description             smtp
        check_command                   check_smtp
        }
